# Manifest Notes

Keep pinned or custom manifest snapshots here when the upstream manifest URL is not stable enough for CI.

For simple GKI branches, prefer remote manifests in `configs/targets/*.toml` and let the workspace metadata record the resolved source.
